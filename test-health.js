const https = require("https"); const options = { hostname: "backend-vijay19.vercel.app", port: 443, path: "/health", method: "GET" }; const req = https.request(options, (res) => { let data = ""; res.on("data", (chunk) => { data += chunk; }); res.on("end", () => { console.log("Status:", res.statusCode); console.log("Response:", data); }); }); req.on("error", (e) => { console.error("Error:", e.message); }); req.end();
