const https = require("https"); const formData = "--boundary----\r\nContent-Disposition: form-data; name=\"image\"; filename=\"test.jpg\"\r\nContent-Type: image/jpeg\r\n\r\ntest"; const options = { hostname: "backend-vijay19.vercel.app", port: 443, path: "/api/v1/upload", method: "POST", headers: {"Content-Type": "multipart/form-data", "Content-Length": Buffer.byteLength(formData)} }; const req = https.request(options, (res) => { let data = ""; res.on("data", (chunk) => { data += chunk; }); res.on("end", () => { console.log("Status:", res.statusCode); console.log("Response:", data); }); }); req.on("error", (e) => { console.error("Error:", e.message); }); req.end(formData);
